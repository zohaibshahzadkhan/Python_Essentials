# An alternative is to store some or all functions in separate Python files.These files are called modules.
class function:
  message = 'I am exportable function'
  def exportableFunc():
    print(function.message);
