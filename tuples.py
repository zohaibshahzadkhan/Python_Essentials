# A tuple is like a list, but the elements are fixed. They can't be changed—unless you redefine the whole tuple.
# use parentheses instead of square brackets.
# You pick a particular element out of a tuple the same way you pick an element out of a list

fixed_attributes = (25, "4250155963794");
print(fixed_attributes[:2]);